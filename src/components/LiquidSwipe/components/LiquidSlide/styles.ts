import styled from 'styled-components';
import { motion } from 'framer-motion';

export const LiquidSlidePath = styled(motion.path)`
  /* fill: rgb(255, 191, 241); */
`;

export const Handle = styled(motion.circle)`
  fill: transparent;
  cursor: grab;
`;
